---
title: VidyaRAG
emoji: 📚
colorFrom: green
colorTo: indigo
sdk: gradio
app_file: app.py
pinned: false
license: mit
short_description: A study assistant that admits when the textbook lacks the answer
---

# VidyaRAG

**A study assistant that checks its own work — and admits when the textbook
doesn't have the answer.**

Retrieval-augmented QA over two OpenStax textbooks — *Biology* and *Anatomy and
Physiology*, both CC BY 4.0 — with page-level citations, a claim-level
groundedness self-check, and explicit abstention when the corpus cannot support
an answer.

Every response shows its own working: per-stage latency, token counts,
list-price cost, and whether the guardrails or the self-check loop intervened.

## Try the last two examples

They are the point of the project:

- **A plausible question the books do not cover.** The system refuses instead of
  inventing an answer. On a held-out set of 12 such questions it refuses 12/12,
  where the baseline refused 0/12.
- **A prompt-injection attempt.** Blocked before retrieval runs, so it costs
  nothing.

## What the numbers say

| | baseline | shipped |
|---|---:|---:|
| Abstention recall | 0.000 | **1.000** |
| Abstention precision | — | 0.800 |
| Recall @context | 0.880 | 0.913 |
| MRR | 0.770 | 0.830 |

Measured on a 58-question gold set. The full methodology — including a
documented negative result, a measured noise floor, and an honest statement that
the self-check does *not* improve answer quality but removes answers that should
not have been given — is in the
[repository](https://github.com/NehaBharti08/VidyaRAG).

## Caveats

Answers are generated by a language model and can be wrong even when
well grounded. This is a study aid, not a reference.

Corpus © OpenStax, Rice University, CC BY 4.0. This project is not affiliated
with or endorsed by OpenStax.
